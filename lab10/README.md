📘 Лабораторная №10 — Модульность JavaScript

Этот проект демонстрирует основы модульности в JavaScript (ES Modules).
Приложение выполняет несколько операций с процентами и выводит красиво отформатированные результаты на странице.

🚀 Функциональность

Приложение позволяет:

вычислить процент от числа;

вычислить число, увеличенное на процент;

вычислить число, уменьшенное на процент;

красиво отформатировать результаты;

вывести данные в HTML-интерфейс.

Все части проекта разнесены по отдельным модулям, чтобы сформировать структуру реального приложения.

📁 Структура проекта
project/
│
├── index.html
├── main.js
├── mathUtils.js
├── formatter.js
└── ui.js

index.html

Главная HTML-страница, подключающая модуль main.js:

<script type="module" src="./main.js"></script>


Внутри страницы есть контейнер <main id="app"></main>, в который динамически вставляется интерфейс.

📦 Описание модулей
1. mathUtils.js

Модуль содержит математические функции для работы с процентами:

export function percentOf(num, percent);
export function addPercent(num, percent);
export function subtractPercent(num, percent);


Используется только в main.js.

2. formatter.js

Утилита для форматирования чисел и итоговых сообщений:

export function formatNumber(value, decimals = 2);
export function formatResult(label, value);


Отвечает за красивое отображение чисел с фиксированным количеством знаков после запятой.

3. ui.js

Модуль, отвечающий за интерфейс:

генерирует HTML-форму внутри <main id="app">;

возвращает элементы, необходимые для работы;

выводит результаты на страницу.

Основные функции:

export function createAppRoot();
export function showResults(container, lines = []);

4. main.js

Главный модуль приложения. Делает следующее:

Импортирует функции из остальных модулей.

Создаёт интерфейс через createAppRoot().

Добавляет обработчик отправки формы.

Производит вычисления:

процент от числа,

число + процент,

число − процент.

Форматирует строки через formatResult().

Выводит результат через showResults().

Ключевая функция:

function renderResults(num, percent) { ... }

▶️ Как запустить
Вариант 1 — открыть напрямую (подходит для современных браузеров)

Просто откройте index.html двойным кликом.
Современные браузеры поддерживают ES-модули без сервера.

Вариант 2 — через локальный сервер (если браузер блокирует модули)

Используйте любой простой сервер, например:

Через VS Code:
Live Server → Open with Live Server

Через Node.js:
npx serve


Перейти по адресу:

http://localhost:3000

🧪 Пример работы

Ввод:

число: 200

процент: 15

Вывод:

15% от числа 200: 30.00
200 + 15%: 230.00
200 - 15%: 170.00

🎓 Чему учит работа

Правильно разбивать код на модули.

Использовать export / import.

Организовывать логику по слоям:

вычисления,

форматирование,

интерфейс,

связующий модуль.

Работе с DOM через JavaScript.

Созданию мини-приложений на чистом JS.
