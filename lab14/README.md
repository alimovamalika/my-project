🧪 Лабораторная 14 — Тестирование и отладка JavaScript

В этой работе реализованы несколько JS-функций и демонстрационный HTML-интерфейс, а также написаны unit-тесты через Jest.
Цель лабораторной — научиться тестировать функции, находить ошибки и анализировать их поведение.

📌 Содержание проекта

HTML-страница, запускающая функции и выводящая результаты.

Три JavaScript-функции:

sumOfSquares

reverseString

умышленно сломанная findMinBroken

Набор Jest-тестов.

Конфигурация npm для запуска тестов (package.json).

📁 Структура проекта
/
├── index.html
├── utils.js
├── main.js
├── utils.test.js
└── package.json

📄 Описание файлов
index.html

Простая веб-страница с кнопкой и блоком <pre> для вывода результатов.

<button onclick="runDemo()">Запустить функции</button>
<pre id="output"></pre>


Подключает:

<script src="utils.js"></script>
<script src="main.js"></script>

main.js

Содержит функцию runDemo(), которая:

вызывает JS-функции;

записывает результаты в консоль на странице;

показывает работу ошибочной функции findMinBroken.

Пример вывода:

=== Демонстрация функций ===
sumOfSquares([1,2,3]) = 14
reverseString("HELLO") = OLLEH
findMinBroken([5,1,-3,7]) → 7 (ошибка)

utils.js

Функции для тестирования.

✔ Правильные функции:
sumOfSquares(arr);
reverseString(str);

⚠ Ошибочная функция:
function findMinBroken(arr) {
    let min = 0;
    arr.forEach(n => {
        if (n > min) {
            min = n;
        }
    });
    return min;
}


Ошибка: ищет максимум среди положительных чисел, а не минимум.

utils.test.js

Jest-тесты для всех функций.

expect(sumOfSquares([1,2,3])).toBe(14);
expect(reverseString("ABC")).toBe("CBA");
expect(findMinBroken([5,1,-3])).toBe(-3); // тест должен упасть


Тест для findMinBroken специально проваливается — это демонстрация неправильного поведения функции.

package.json

Конфигурация npm + Jest:

{
  "name": "lab14",
  "version": "1.0.0",
  "description": "Тестирование и отладка JS функций",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}

▶️ Как запустить проект
1. Установить зависимости
npm install

2. Запустить тесты
npm test


Ожидаемый результат:

тесты sumOfSquares и reverseString проходят

тест findMinBroken — падает (и это правильно)

🎯 Цель лабораторной

Освоить юнит-тестирование JavaScript-функций.

Понять, как Jest определяет корректность функций.

Научиться находить ошибки на практике.

Научиться работать с npm, test-скриптами и структурой проекта.

📌 Возможные улучшения

При желании можно:

исправить findMinBroken и сравнить тесты «до» и «после»;

подключить покрытие кода (coverage);

добавить больше тест-кейсов;

сделать визуальную подсветку результатов на странице.
